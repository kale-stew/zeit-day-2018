{
  "name": "machine-learning-with-javascript",
  "version": "0.1.0",
  "private": true,
  "homepage" : "https://kale-stew.github.io/zeit-day-2018",
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4",
    "spectacle": "^4.2.2"
  },
  "scripts": {
    "build": "npm run clean && react-scripts build",
    "clean": "rm -rf ./build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject",
    "start": "react-scripts start",
    "test": "react-scripts test --env=jsdom"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
